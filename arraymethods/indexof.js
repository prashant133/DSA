// indexof
// ->   returns the first index (position) of a specified value.
// -> method returns -1 if the value is not found.

const arr = [1, 2, 3, 4, 2, 5];

const index1 = arr.indexOf(3);
const indext2 = arr.indexOf(4);
